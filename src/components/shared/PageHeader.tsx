// Shared design system — promovido do /super-admin para uso global.
// Reuso direto do componente PageHeader já estabelecido no SA.
// Mantém o padrão Linear/Vercel: eyebrow opcional, título tight,
// descrição discreta, ações alinhadas à direita.

export { PageHeader, PageSkeleton } from "@/components/superadmin/PageHeader";
export type { PageHeaderProps } from "@/components/superadmin/PageHeader";
